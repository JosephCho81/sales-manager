# 판매관리 시스템 — 전체 스펙 문서
> 최초 구축용 요구사항 문서. 이후 운영하며 확정된 규칙은 각 절의 **[운영 반영]** 블록에 덧붙인다.
> 코드 구조·수정 지점은 `CLAUDE.md`, 개별 사건의 상세는 `docs/`가 최신이다.
> 세 문서가 어긋나면 **`CLAUDE.md` → `docs/` → 이 문서** 순으로 우선한다.

---

## 1. 프로젝트 개요

### 목적
한국에이원 / 금화 / 라성 3개 회사의 매입·매출·마진을 한 곳에서 관리.
입고 물량과 단가를 입력하면 각사 마진·매출·계산서 발행 지시가 자동 계산된다.

### 기술 스택
- **Frontend + Backend:** Next.js (App Router)
- **DB + Auth:** Supabase (PostgreSQL + Supabase Auth)
- **배포:** Vercel
- **언어:** TypeScript

### 사용자 *(2026-08-11 개정 — 계정 분리 및 권한 도입)*
- **계정 3개** (Supabase Auth, 아이디 → 내부 이메일 매핑 `lib/account.ts`)

  | 아이디 | 표시 이름 | 역할 | 권한 |
  |---|---|---|---|
  | `cho` | (주)금화 조중호 대표님 | `owner` | 입력·수정·삭제 |
  | `kim` | (주)나성 김주종 대표님 | `viewer` | 조회 전용 |
  | `choi` | (주)한국에이원 최성호 대표님 | `viewer` | 조회 전용 |

- **역할 저장:** `user_roles` 테이블(`owner` / `viewer`). 행이 없으면 접근 거부(기본 거부).
- **권한 경계:** 서버 액션의 `requireOwner()` **하나뿐**. 화면의 버튼 숨김(`useCanEdit()`)은 편의일 뿐 방어선이 아니다.
- **로그인 유지:** 세션 쿠키 400일(httpOnly·SameSite=Lax, 프로덕션은 Secure). 로그인·로그아웃은 서버 액션 전용.
- **보안:** 로그인 없이 접근 불가(미들웨어 `proxy.ts` + 레이아웃 2중 가드). 모든 DB 접근은 service-role 서버 경유이며, `authenticated` 롤에는 테이블 권한을 주지 않는다(RLS 정책 0개).
- **변경 이력:** 모든 쓰기는 `audit_log`에 실행 계정과 함께 기록된다.

---

## 2. 관리 대상 회사

| 구분 | 회사명 | 역할 |
|------|--------|------|
| 핵심 3사 | 한국에이원 | 메인 중개사. 모든 거래의 중심. |
| 핵심 3사 | 금화 | 커미션 수취 / 일부 거래 체인 참여 |
| 핵심 3사 | 라성 | 커미션 수취 |
| 매입처 | 화림 | 제조사. AL35B/65B/AL30 공급 |
| 매입처 | EG | 페로실리콘 수입사 (중국). 달러 결제 |
| 매입처 | 렘코 | 소괴탄/분탄 관련 |
| 매입처 | 동창 | 분탄 관련 |
| 거래처 | 동국제강 | 주요 납품처 |
| 거래처 | 현대제철 | AL30 납품처 |
| 기타 | 호진 | AL35B 물량 배분 관련 업체 |

---

## 3. 공통 마진 배분 룰

**전 품목 공통 적용:**
- 총 마진을 한국에이원 / 금화 / 라성이 1/3씩 나눔
- 소수점 나머지(나누어 떨어지지 않는 경우)는 라성이 가져감
- 예: 100원 → 한국에이원 33원 / 금화 33원 / 라성 34원
- 한국에이원이 금화·라성에 커미션 형태로 지급
- **상무님 배분은 폐지. 시스템에 포함하지 않음**

---

## 4. 품목별 거래 구조

### 4-1. AL-35B / AL-65B (동국제강)

**거래 체인:** 동국제강 → 한국에이원 → 금화 → 화림

**입찰:** 기간별 낙찰. 낙찰 단가는 화림에서 제공. 6개월 단위.

**계산서 발행:**
- 동국제강 → 한국에이원: 익월 1일, 당월말 기준 **역발행**
- 금화 → 한국에이원: 익익월 1일, 익월말 기준 발행
- 화림 → 금화: **당월말 기준** 계산서 발행 (대금 수령은 익익월 1일)

**대금 흐름:**
- 동국제강이 한국에이원에 익월 말일 현금 지급
- 익익월 1일: 한국에이원 → 금화 → 화림 순차 지급

**커미션:**
- 라성: 익익월 10일, 한국에이원에서 계산서 발행 후 현금 수취

**VAT:** 10% 적용

**호진 배분 (AL35B 특이사항):**
- 화림이 물량 배분 단가 결정 (예: 100톤 중 80톤 100원, 20톤 20원)
- 호진이 물량을 더 가져갈 때: 화림 → 한국에이원 커미션 지급
  - 계산서: 당월말 기준 (익월 2~3일에 정리 후 당월말 기준으로 발행)
  - 한국에이원 → 금화·라성 배분: 익월 10일
- 호진이 물량을 덜 가져갈 때: 한국에이원 → 호진 배분 지급 (화림이 단가 결정)

**마진 계산:**
- 총 마진 = (동국 판매단가 - 화림 원가단가) × 물량
- 1/3씩 배분 (라성이 나머지)

**[운영 반영] 2026-07~ 화림 매입단가 2개 (2026-08-10 확정)**
- 매출단가는 1개, **화림 매입단가만 2개**다. 계약을 2건 등록하고 같은 납품을 단가별 2건으로 입력한다.
- 물량은 현대 AL(반반)과 달리 **화림 매입 실톤수대로 배분**한다.
  - 2026-07 예: 236,500원 × 202.905톤 + 330,000원 × 130.846톤 (합 333.751톤)
- 두 계약의 `invoice_month_offset`은 **반드시 같게**(현재 2). 다르면 집계에서 한쪽이 통째로 누락된다.
- 코드 수정은 필요 없다 — 매출·매입·금화 공급가·마진이 모두 건별 합산이다.
- 금화→한국에이원 공급가는 건별 `(원가 + floor(톤당마진/3)) × 톤` 합산 후 1회 반올림.
  금화가 2줄짜리 계산서로 라인별 반올림하면 1원 차이가 날 수 있어 첫 발행월 실물 대사가 필요하다.

---

### 4-2. 소괴탄 (동국제강)

**거래 체인:** 동국제강 → 한국에이원 → 렘코

**계산서 발행:**
- 동국제강 → 한국에이원: 익월 1일, 당월말 기준 발행
- 한국에이원 → 렘코: 동일 기준 발행

**대금 흐름:**
- 동국제강이 익월 10일 한국에이원에 현금 지급 (휴일이면 **다음** 영업일)
- 한국에이원 → 렘코: 익월 10일 당일 즉시 지급

**커미션:** *(2026-08-11 개정 — 15일 → 10일)*
- 금화·라성: **익월 10일** 커미션 지급. 지급일이 휴일이면 **직전 영업일로 앞당김**
  (대금과 보정 방향이 반대다 — 대금은 뒤로, 커미션은 앞으로)

**VAT:** 없음

---

### 4-3. 분탄 (렘코)

> 동국제강 없음. 매입처=동창, 매출처=렘코.

**거래 체인:** 동창 → 한국에이원 → 렘코

**계산서 발행:**
- 익월 1일, 당월말 기준으로 체인 전체 동시 발행
  (렘코 역발행→한국에이원 / 한국에이원→동창 동시)

**대금 흐름:**
- 렘코가 익월 10일 한국에이원에 지급 (휴일이면 **다음** 영업일)
- 익월 10일 당일: 한국에이원 → 동창 지급

**커미션:** *(2026-08-11 개정 — 15일 → 10일)*
- 금화·라성: **익월 10일** 커미션 지급. 지급일이 휴일이면 **직전 영업일로 앞당김**

**VAT:** 10% 적용

**[운영 반영] 월별 감가 — 렘코 상장 대응 (2026-07-06 확정)**
- 동국제강이 감가를 **월말 일괄 통보**한다(건별 아님). `monthly_depreciations` 테이블(품목×납품월)에 입력.
- 렘코가 총액 기준 매출을 유지해야 하므로 **동창 매입 계산서만 차감**한다. 렘코 역발행 매출은 총액.
- 커미션·마진 배분도 **총액 기준**. 계산서 금액 차이로 계산하면 감가까지 3사에 배분돼 반환 재원이 사라진다.
- 감가는 한국에이원 통장에 미배분 보관 → **계약 종료 후 렘코에 반환**. 최종 부담자는 동창.

**[운영 반영] 동창 부가세 끝자리는 공식으로 못 맞춘다 (2026-08-10 확정)**
- 2026-06 납품분: 라인별 절사 합산 `floor(총액×0.1) − floor(감가×0.1)` = 39,233,044 가 실물
- 2026-07 납품분: 차감 후 일괄 절사 `floor(차감후×0.1)` = 37,061,476 이 실물
- 소수부 구조가 같은데 결과가 반대 → **하나의 공식으로 두 달을 못 맞춘다. 공식을 또 바꾸지 말 것.**
- `monthly_depreciations.cost_vat_actual`에 실물 세액을 넣으면 계산값을 덮어쓴다. NULL이면 기존 계산 유지(과거 월 불변).
- 감가 패널의 `부가세 실계산서` 입력란이 이 값이다. AL30(화림) 매입 계산서에도 같은 방식으로 적용된다.

---

### 4-4. 페로실리콘 — FeSi75 / FeSi60 (동국제강)

> FeSi75와 FeSi60은 별도 품목으로 각각 독립 관리.
> 입찰 결과에 따라 둘 다 낙찰, 둘 다 미낙찰, 하나만 낙찰 가능.

**거래 체인:** EG(중국) → 한국에이원 → 동국제강

**입찰:** 6개월 단위. EG 매입 단가 고정 (입찰 시 확정).

**환율:**
- BL(선하증권) 기재 날짜 기준 환율 적용
- 동국제강이 환율 결정. 동일 환율을 EG와의 거래에도 적용
- 환차손익 관리 불필요

**계산서 발행:**
- 동국제강 → 한국에이원: 입고 확인 후 **역발행** (BL 날짜 기준 환율 적용)
- EG → 한국에이원: 동국 역발행 계산서 발행과 동일 날짜 기준으로 발행. 달러 계산서.

**대금 흐름:**
- 동국제강 → 한국에이원: 입고 후 10일 이내 현금 지급
- 한국에이원 → EG: 입고 후 15일 이내 현금 지급 (달러 송금)

**마진 계산:**
- 총 마진 = (동국 판매금액 원화) - (EG 매입금액 원화 환산)
- 1/3씩 배분 (라성이 나머지)
- 한국에이원 → 금화·라성 커미션 지급 타이밍: EG 지급 완료 후

**VAT:** 동국 역발행 계산서 발행일 기준 동일 날짜 적용

---

### 4-5. AL-30 (현대제철)

**거래 체인:** 화림 → 한국에이원 → 현대제철

**입찰:** 6개월 단위. 낙찰 단가 = 현대제철 판매단가. 화림 매입단가는 한국에이원-화림 협의.

**계산서 발행 (마진 부분):**
- 현대제철 → 한국에이원: **10일 단위 역발행** (월 3장). 60일 어음 발행.
- 한국에이원 → 화림: 당월 3장 합산하여 **1장**으로 당월말 기준 계산서 발행
  - 예: 1회 100원 + 2회 100원 + 3회 100원 → 화림에 300원 계산서 1장

**대금 흐름 (마진 부분):**
- 어음 만기: 계산서 발행일 기준 정확히 60일
- 어음 현금화 후 익익월 1일 화림에 현금 지급
- 마진 = (현대제철 매출단가 × 물량) - (화림 매입단가 × 물량)
- 익익월 기준 계산서 발행 후 익익월 10일 금화·라성에 1/3씩 지급

**부족분 커미션:**
- 화림이 매월 부족분 물량·단가를 결정해서 한국에이원에 통보
- 화림 → 한국에이원: 당월 기준 계산서 발행 후 5일 내 현금 지급
- 마진 1/3 룰 동일 적용
- 한국에이원 → 금화·라성: 익월 기준 계산서 발행 후 익월 10일 각각 현금 지급

**화면 표시:**
- 마진(익익월 기준)과 커미션(당월 기준)을 **같은 화면에서 월별로 함께 표시**
- 비고란에 "N월 마진" / "N월 부족분 커미션" 명시

**VAT:** 10% 적용

**[운영 반영] AL40 고품위알믹스 추가 · 화림 매입단가 2개 (반반 분할)**
- 현대제철 납품 품목은 AL30 외에 **AL40고품위알믹스**가 있다(DB 품목명 그대로, 표시명 AL-40).
- 두 품목 모두 매출단가 1개 / **화림 매입단가 2개**. 같은 납품을 **물량 반반**으로 나눠 2건 입력한다.
- 두 계약의 `invoice_month_offset`을 **반드시 같게**(현재 2). 다르게 뒀다가 매출마진 현황에서
  AL40 절반이 통째로 누락된 사고가 있었다(2026-07-31 수정). 집계·계산서 모두 `invoice_month` 하나로만 판단한다.

**[운영 반영] AL30 통과형 감가 — 현대 감액 발행 → 화림 회수 (2026-05분)**
- 현대제철은 감가를 **통보 없이 반영해 역발행**한다. 우리 저장액보다 실입금이 적어야 알게 된다.
- `monthly_depreciations`의 `sales_deduct_ym`(매출 감액 납품월) / `cost_deduct_ym`(매입 회수 납품월)로 표현.
- 매출 감액월에 매출·마진·커미션이 함께 줄고, 매입 회수월에 되돌아온다. 상세는 `docs/al30-depreciation-2026-05.md`.
- 감가 반영 계산서의 부가세는 **매출·매입·커미션 모두 라인별**로 계산한다(차감 후 일괄 10%는 1원 어긋남).

---

## 5. 계산서 발행 지시 기능

### 동작 방식
1. 입고 데이터 입력 시 백그라운드에서 자동 계산 항상 유지
2. 관리자가 "발행 지시 생성" 버튼을 누를 때 해당 월 지시 목록 확정
3. 지시 목록: **누구에게 / 얼마 / 언제까지** 자동 생성

### 지시 목록 항목
- 발행 대상 회사
- 수취 회사
- 금액 (공급가액 / VAT / 합계)
- 발행 기준일
- 발행 마감일
- 지급 예정일
- **지급 완료 체크박스** (체크 시 미지급 잔액에서 제외)

### 미지급 잔액 추적
- 지급 완료 체크가 안 된 건은 미지급으로 집계
- 대시보드에서 회사별 미지급 잔액 한눈에 확인

---

## 6. 화면 구성 (페이지)

### 6-1. 대시보드 (메인)
- 이번 달 전체 매출 / 총 마진 요약
- 한국에이원 / 금화 / 라성 각사 이번 달 배분 마진
- 미지급 잔액 현황 (회사별)
- 이번 달 계산서 발행 지시 목록 (지급 완료 체크 포함)
- 이번 달 대금 수령·지급 일정 타임라인

### 6-2. 품목별 입고 입력
- 월 선택
- 품목 선택
- 적용 낙찰 단가 선택 (해당 기간 자동 필터)
- 물량 입력 (같은 달 복수 단가는 건별로 나눠 입력)
- 입력 시 마진 실시간 미리보기

### 6-3. 낙찰 단가 관리
- 품목별 낙찰 기간 / 판매단가 / 원가단가 등록
- 기간 겹침 방지 validation
- 페로실리콘은 달러 단가 + 환율 입력

### 6-4. 각사 매출·마진 현황
- 월별 / 품목별 필터
- 한국에이원 / 금화 / 라성 3사 나란히 표시
- 매입 / 매출 / 마진 / 커미션 수취 각각 표시
- 현대제철 마진·커미션 같은 화면에서 월별로 표시 (비고 포함)

### 6-5. 계산서 발행 지시 목록
- 월별 전체 발행 지시 목록
- 지급 완료 체크
- 미지급 잔액 집계

### 6-6. 거래처별 지급 이력
- 렘코 / 동창 / EG / 화림 각각
- 지급 이력 + 미지급 잔액
- EG는 달러·원화 병행 표시

### 6-7. 현대제철 전용 뷰
- 10일 단위 계산서 3장 전체 표시 (합산 아닌 건별)
- 월 소계 별도 표시
- 60일 어음 만기일 자동 계산 표시
- 부족분 커미션 입력 및 표시

---

## 7. DB 스키마 (Supabase PostgreSQL)

> 아래는 최초 설계안이다. 운영하며 컬럼·테이블이 늘었으므로 **실제 스키마는
> `supabase/migrations/`의 `001`~최신 파일을 순서대로 읽은 것이 정답**이다.
> 주요 추가분은 이 절 끝의 "[운영 반영] 이후 추가된 테이블·컬럼"에 정리한다.

### products (품목)
```
id, name, display_name, buyer (납품처), unit, price_unit,
chain (jsonb), invoice_rule (jsonb), payment_rule (jsonb),
commission_rule (jsonb), vat, memo, is_active
```

### contracts (낙찰 단가 계약)
```
id, product_id, start_date, end_date,
sell_price, cost_price, currency (KRW/USD),
exchange_rate_basis, margin_distribution (jsonb), memo
```

### deliveries (납품 건)
```
id, year_month, product_id, contract_id,
quantity_kg, addl_quantity_kg, addl_margin_per_ton,
memo, created_at
```

### invoice_instructions (계산서 발행 지시)
```
id, year_month, product_id, delivery_ids (jsonb),
from_company, to_company,
supply_amount, vat_amount, total_amount,
invoice_basis_date, issue_deadline, payment_due_date,
is_paid, paid_at, memo
```

### hyundai_transactions (현대제철 전용)
```
id, year_month, invoice_date, invoice_seq (1~3),
quantity_kg, sell_price, cost_price,
supply_amount, vat_amount,
bill_due_date (발행일 +60일),
commission_amount, commission_type (margin/shortage),
margin_distribution (jsonb), memo
```

### fx_rates (환율 기록 - 페로실리콘용)
```
id, bl_date, product_id, rate_krw_per_usd, memo
```

### [운영 반영] 이후 추가된 테이블·컬럼

**추가 테이블**

| 테이블 | 용도 |
|---|---|
| `commissions` | 동국제강·현대제철 부족분 커미션 입력 (`year_month, company, quantity_kg, price_per_ton, commission_amount`). `hyundai_transactions`를 대체 — 신규 입력은 이쪽 |
| `monthly_depreciations` | 품목×납품월 월별 감가. 분탄(보관형)·AL30(통과형) 공용 |
| `expenses` | 공동 경비 (`date, description, amount, payer, is_settled`) |
| `audit_log` | 서버 액션 변경 이력 (`actor_email, table_name, row_id, action, before, after`) |

**추가 컬럼 (주요)**

| 테이블.컬럼 | 의미 |
|---|---|
| `contracts.invoice_month_offset` | 납품월 → 청구월 오프셋. **집계·계산서의 유일한 기준**(`invoice_month = year_month + offset`) |
| `contracts.reference_exchange_rate` | USD 계약 참고환율. 없으면 마진 계산이 throw |
| `contracts.supersedes_contract_id` / `revision_reason` / `revised_at` | 계약 중도 정정 이력 |
| `contracts.cost_price_2` | **미사용** (구 설계 잔재 — 매입단가 2개는 계약 2건으로 표현한다) |
| `deliveries.invoice_month` | 청구월. 계산서·집계가 이 값 하나로만 판단 |
| `deliveries.delivery_date` | AL30 10일 단위 구간 분류용 |
| `deliveries.depreciation_amount` | **건별** 감가 (소괴탄 전용. 분탄은 `monthly_depreciations`로 이동) |
| `invoice_instructions.invoice_type` | `sales` / `cost` / `commission` |
| `invoice_instructions.delivery_year_month` | 납품월. `year_month`(지급월)와 다르다 |
| `invoice_instructions.paid_amount` | 실입금/실지급액. NULL이면 `total_amount`와 동일 |
| `monthly_depreciations.sales_deduct_ym` / `cost_deduct_ym` | 매출 감액 납품월 / 매입 차감·회수 납품월 |
| `monthly_depreciations.cost_vat_actual` | 감가 반영 **매입** 계산서의 실물 부가세. NULL이면 계산값 |
| `invoice_instructions.actual_supply_amount` / `actual_vat_amount` / `reconciled_at` | 실물 세금계산서 대사값. NULL = 미대사 (020) |
| `user_roles.role` | `owner`(편집) / `viewer`(조회 전용). 행 없음 = 접근 거부 (017) |
| `holidays.date` | 음력·대체공휴일. 고정 공휴일은 코드(`FIXED_HOLIDAYS_MD`)에 있다 (018) |
| `*.updated_at` | 낙관적 잠금 기준. DB 트리거가 자동 갱신 (019) |

> `hyundai_transactions`는 초기 설계 테이블로 과거 데이터가 남아 있으나 현재 코드는 쓰지 않는다.

---

## 8. 개발 우선순위

1. **1단계 (핵심):** 로그인 + 품목 설정 + 낙찰 단가 등록 + 입고 입력 + 마진 자동 계산
2. **2단계 (운영):** 계산서 발행 지시 생성 + 지급 완료 체크 + 미지급 잔액
3. **3단계 (분석):** 각사 매출·마진 현황 대시보드 + 거래처별 지급 이력
4. **4단계 (특수):** 현대제철 전용 뷰 + 페로실리콘 달러 처리 + 호진 배분

*(1~4단계 완료. 이하 2026-08-11 추가분)*

5. **운영 안전장치:**
   - 계정 3개 + 역할 분리(owner/viewer), RLS 잠금
   - 공휴일 DB화 — 지급일이 등록 안 된 연도로 넘어가면 화면에 경고 (배포 없이 수정 가능)
   - 낙관적 잠금 — 오래된 화면에서 저장 시 덮어쓰기 거부
   - 실물 계산서 대사 — 생성값과 실물의 차이를 기록·표시

---

## 9. 운영 도구

| 명령 | 용도 |
|---|---|
| `npm run check:schema` | 마이그레이션 적용 상태 점검 (읽기 전용). **배포 전 필수** |
| `npm run seed:users -- "<비밀번호>"` | 계정 생성·비밀번호 재설정 (비밀번호는 소스에 넣지 않는다) |
| `npm test` | 계산 로직 회귀 테스트 |

마이그레이션은 CLI 연동 없이 Supabase SQL Editor에서 **번호순 수동 실행**한다.
새 컬럼에 의존하는 코드는 마이그레이션 적용 **후에** 배포해야 한다 — 순서가 바뀌면
"저장이 안 된다"는 증상으로 나타난다.

---

## 9. 추후 추가 예정
- AL90 탈산제 (테스트 완료 후 품목 추가)
